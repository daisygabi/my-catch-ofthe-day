import React from 'react';
import {shallow} from 'enzyme';
import StorePicker from '../StorePicker';

it('renders without crashing', () => {
    shallow(<StorePicker/>);
});

test('Show left sidebar', () => {

});